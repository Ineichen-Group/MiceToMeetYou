[project]
name = "mice-to-meet-you"
version = "0.1.0"
description = ""
requires-python = ">=3.13"
dependencies = [
    "django>=6.1",
    "psycopg[binary]>=3.2"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
